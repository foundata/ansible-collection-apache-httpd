---
trivial:
  - >
    ``run`` role - Internal refactor: the effective merged variables
    ``__run_apache_httpd_effective_main_directives``,
    ``__run_apache_httpd_effective_snippet_settings`` and
    ``__run_apache_httpd_effective_modules_enabled`` are now computed lazily in
    ``vars/main.yml`` instead of via ``set_fact`` tasks in ``tasks/init.yml``.
    No user-facing behaviour change. Added Molecule init-only regression tests
    asserting the merges.
